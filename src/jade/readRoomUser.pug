include general/_

doctype html
html(lang='ru')
    +head
    body

        +header

        +b.MAIN.main
            +panel
            +b.wrapUserBooksInfo
                +listBook('Прочитанные книги', false)
                +listBook('Избранное', false)
                +listBook('Книги на руках', true)
        script(src="js/vendor.min.js")
        script(src="js/vendor2.min.js")
        script(src="js/main.js")
